
const KEYBOARD_TYPE = {
  deafult: "default",
  email: "email-address",
  numeric: "numeric",
  phone: "phone-pad",
  number: "number-pad",
  decimal: "decimal-pad"
};

const STATUSBAR_CONTENT = {
  default: "default",
  dark: "dark-content",
  light: "ight-content"
};


const TEXTFILED = {
  MAX: 50
};

const DATE_FORMAT = {
  DD_MM_YYYY: "DD/MM/YYYY",
  YYYY_MM_DD: "YYYY-MM-DD",
};

const PAGINATION_SIZE = {
  size: 20
};


export {
  KEYBOARD_TYPE,
  STATUSBAR_CONTENT,
  TEXTFILED,
  DATE_FORMAT,
  PAGINATION_SIZE,
};
